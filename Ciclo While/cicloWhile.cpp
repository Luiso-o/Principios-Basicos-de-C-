#include <iostream>

int main() {
    int contador = 1;

    while (contador <= 5) {
        std::cout << "Contador: " << contador << std::endl;
        contador++; // Incrementa el valor de contador
    }

    return 0;
}
